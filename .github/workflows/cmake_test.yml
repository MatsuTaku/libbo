name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Run a multi-line script
        run: |
          cmake -B build .
          cmake --build build
          cd build
          ctest
